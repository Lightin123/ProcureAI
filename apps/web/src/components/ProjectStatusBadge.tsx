import { GovernmentStatusBadge, type GovernmentStatusBadgeProps } from "./GovernmentStatusBadge.js";

export function ProjectStatusBadge(props: GovernmentStatusBadgeProps) {
  return <GovernmentStatusBadge {...props} />;
}
